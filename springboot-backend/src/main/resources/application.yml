spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/SalesDB
    username: postgres
    password: password

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8080

# We'll wire this later (security/CORS profiles)
app:
  cors:
    allowed-origins: "http://localhost:3000"
